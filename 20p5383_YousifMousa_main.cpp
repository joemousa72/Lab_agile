#include <iostream>
using namespace std;
int sum(int x, int y) {
	return x + y;
}
int main() {
	int num1, int num2;
	cout << "Please enter two integers:";
	cin >> num1 >> num2;
	cout << "The sum is:" << sum(num1 + num2)
		return 0;
}